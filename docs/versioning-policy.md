# Versioning Policy

- Platform workflows are consumed by tags, not branches.
- Tag strategy:
  - `v1` rolling major tag
  - `v1.x.y` immutable release tags
- Breaking changes require:
  1. new major tag (`v2`)
  2. migration notes
  3. pilot validation
